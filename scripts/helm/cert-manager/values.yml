---
# Docs
## https://github.com/cert-manager/cert-manager
## https://cert-manager.io/docs/
## Feature gate Gateway API: https://cert-manager.io/docs/usage/gateway/

# Init
## helm repo add jetstack https://charts.jetstack.io
## helm repo update

# Deploy or Upgrade (fresh install)
## helm upgrade --install cert-manager jetstack/cert-manager \
##   --namespace kube-gateway \
##    --version v1.13.1 \
##   --set "extraArgs={--feature-gates=ExperimentalGatewayAPISupport=true}" \
##   --set crds.enabled=true

# Note: If already installed, add this command after:
## kubectl rollout restart deployment cert-manager -n kube-gateway
